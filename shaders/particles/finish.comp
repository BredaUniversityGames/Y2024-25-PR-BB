#version 460
#include "particle_vars.glsl"

layout ( local_size_x = 1, local_size_y = 1, local_size_z = 1 ) in;

void main()
{
    uint particleCount = particleCounters.culledCount;

    // TODO: update indirect draw args
}
