#version 460
#include "bindless.glsl"

const uint MAX_DECALS = 64;

struct DecalData
{
    vec3 position;
};

layout (set = 2, binding = 0) uniform DecalsUB
{
    DecalData decals[MAX_DECALS];
};

layout(push_constant) uniform DecalsCount
{
    uint decalsCount;
};

layout (local_size_x = 16, local_size_y = 1, local_size_z = 1) in;

void main()
{
    uint index = gl_GlobalInvocationID.x;
    if (index >= decalsCount)
    {
        return;
    }

    // TODO: decal stuff
}
